server:
  port: 6060
  servlet:
    context-path: /admin

spring:
  profiles:
    active: @profileActive@

app:
  name: admin

logging:
  file:
    path: ./logs
    name: admin
  level:
    # mapper包下打印debug级别日志
    com.yxx.business.mapper: debug
